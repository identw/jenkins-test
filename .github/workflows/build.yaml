name: build
on:
  pull_request:
    types:
      - opened
      - reopened
    branches:    
      - 'master'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: 
        run: |
          ls -la
